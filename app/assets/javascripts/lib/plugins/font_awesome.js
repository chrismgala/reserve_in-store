/*
 * This is ONLY included in development environments.
 * The remote custom compiled JS is included in production instead, but it effectively
 * does the same thing (except the production version is compiled so it's much smaller and faster to load)
 *= require @fortawesome/fontawesome-pro/js/all
 */
