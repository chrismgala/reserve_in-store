<div class="content-heading page-title ">
  <div class="content-heading-row align-items-center justify-content-center">
    <div class="content-heading-left">
        <h1>Plans</h1>
    </div>
  </div>
</div>

<p>
  Showing <strong><%= pluralize(@plans.total_count, 'plan') %></strong>.
</p>
<div class="card">
  <div class="card-body">
  <%= render partial: 'list', locals: { plans: @plans } %>
  </div>
</div>  

<%= paginate @plans %>
