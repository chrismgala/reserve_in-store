<!--Insert the form into the index page and show the modal-->
$('#location-edit-modal').html("<%= escape_javascript( render partial: 'modal', locals: { action: 'edit', location: @location } ) %>").show();
$('#location-edit-container').show();

